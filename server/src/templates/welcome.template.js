export const welcomeTemplate = (name) => {
  return `
    
        <div>
            <h1>Welcome ${name}</h1>

            <p>
                Thank you for registering.
            </p>
        </div>

    `;
};
